# beta.4 合并与发布审查

日期：2026-09-08。目标分支：main。

## 冲突与影响

- 同步后的 origin/main 为 `2832c5ccb32f3ca38009116ded6c794ded648263`，也是优化分支的起点；无远端独立改动需要覆盖或取舍。
- `git merge-tree --write-tree HEAD origin/main` 成功，无文本冲突；推送前再次同步，PR合并时以待审HEAD SHA校验。
- 共享状态恢复、数域/维数验证、坐标矩阵与实际映射、零空间数值判定和移动布局已经在优化阶段审查并回归。发布变更仅增加说明、版本及Actions诊断/发布溯源，不改变数学逻辑。
- 新增求解器schema为v1，原场景schema保持；不删除用户本地存储。回退时应注意新快照与新场景不被旧版本识别。

## 检查与合并条件

本地完整门禁：173项单测、69项桌面/移动端E2E通过，1项桌面不适用的移动专属检查跳过。工作流YAML经格式解析检查，保留完整验收、最小权限、main限定和官方Pages部署链。新增诊断上传Action固定到已核验的v4提交SHA。

合并建议：无阻断项；必须等待此PR的GitHub质量检查成功再合并，不强制推送、不绕过失败检查。采用merge commit保留阶段提交。合并后Pages对合并SHA重新构建和验收。

## 上线核验

核对线上release.json的版本、SHA及运行ID；访问八个场景，检查无资源404或页面异常，操作求解器与实验分享，检查移动调参与主题。线上结果和Actions链接记录在本次交付记录中。当前保留beta状态和Three.js体积提示，未声称覆盖所有真实浏览器。
